import 'package:flutter/material.dart';

class RegisterRaiderPage extends StatelessWidget {
  const RegisterRaiderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GGGGG'),
      ),
    );
  }
}